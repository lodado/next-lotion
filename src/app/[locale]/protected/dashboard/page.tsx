import { AuthButton, LoginDialog } from "@/features";

const Page = () => {
  return (
    <div className="bg-surface-up">
      <AuthButton />
      dummy page
      <LoginDialog />
    </div>
  );
};

export default Page;
