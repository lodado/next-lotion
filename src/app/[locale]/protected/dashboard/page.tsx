import { AuthButton, Editor, LoginDialog } from "@/features";

const Page = () => {
  return (
    <div className="bg-surface-up">
      <AuthButton />
      dummy page
      <Editor />
      <LoginDialog />
    </div>
  );
};

export default Page;
